#include "bits/stdc++.h"
using namespace std;
#define ll long long
int a, b, c, d;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cin >> a;
	cout << (24 < a&&a < 31 ? "ok!" : "no!");
	return 0;
}
