#include "bits/stdc++.h"
using namespace std;
#define ll long long
int n, m, x, y;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cin >> n;
	printf("%d", (int)pow(2, n));
	return 0;
}
