#include "bits/stdc++.h"
using namespace std;
#define ll long long
ll n, m, x, y;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cin >> n >> m;
	printf("%lld", n * m);
	return 0;
}
