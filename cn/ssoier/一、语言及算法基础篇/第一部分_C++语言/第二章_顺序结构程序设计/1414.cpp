#include "bits/stdc++.h"
using namespace std;
#define ll long long
int n, m, x, y, a, b, c, t;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	
	cin >> a >> b >> c;
	cout << a * 0.2 + b * 0.3 + c * 0.5 << endl;
	return 0;
}
