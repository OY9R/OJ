#include "bits/stdc++.h"
using namespace std;
#define ll long long
int n, m, x, y, a, b, c, t;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	
	char c;
	float f;
	double d;
	cin >> c >> n >> f >> d;
	printf("%c %d %.6f %.6lf", c, n, f, d);
	return 0;
}
