#include "bits/stdc++.h"
using namespace std;
#define ll long long
int a, b, c, d;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cin >> a;
	(a > 1 && a < 100) && cout << "yes";
	return 0;
}
